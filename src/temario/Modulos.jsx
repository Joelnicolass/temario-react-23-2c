import React from "react";

/* 

    Es posible trabajar con modulos de node en react.

    IMPORTS:

    (si fue exportado por defecto)
    import NOMBRE from 'RUTA'

    (si fue exportado por nombre)
    import { NOMBRE } from 'RUTA'

    EXPORTS:    

    (exportar por defecto - una por modulo)
    export default NOMBRE

    (exportar por nombre)
    export const NOMBRE = VALOR

*/

const Modulos = () => {
  return <div>Modulos</div>;
};

export default Modulos;
